# === Secrets (a configurer via les variables CI/CD GitLab ou un gestionnaire de secrets) ===
JWT_SECRET=<base64-encoded-secret>
JWT_EXPIRATION_MS=86400000
ADMIN_USERNAME=<admin-username>
ADMIN_PASSWORD=<admin-password>

# --- Registry & Versions pour le Bake ---
REGISTRY_URL=registry.basteproductions.fr/epita_dev_1/fullstack
BACKEND_VERSION=1.0.0
FRONTEND_VERSION=1.0.0
